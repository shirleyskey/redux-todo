import React from 'react'

const Todo = ({ onClick, completed, text }) => (
    <div className="header">
        <center className="center-text">Todo list redux</center>
    </div>
)

export default Todo